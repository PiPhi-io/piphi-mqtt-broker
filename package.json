{
  "name": "piphi-mqtt-broker",
  "version": "0.1.3",
  "private": true,
  "description": "Managed MQTT broker platform service for PiPhi.",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx src/cli.ts --print",
    "print-config": "tsx src/cli.ts --print",
    "generate-config": "tsx src/cli.ts",
    "test": "node --test --import tsx tests/**/*.test.ts"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@types/node": "^22.14.1",
    "tsx": "^4.19.4",
    "typescript": "^5.8.3"
  }
}
